  <section id="${toolId}" name="${toolName}">
    ${foreach tools t}
    <tool file="${toolId}/${t}.xml" />${end}
    ${foreach services s}
    <tool file="${toolId}/${s.serviceId}.xml" />${end}
  </section>
